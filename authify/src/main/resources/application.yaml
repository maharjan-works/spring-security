kind: Application

spring:
  application:
    name: authify

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/authify?createDatabaseIfNotExist=true
    username: root
    password: root

#  jpa properties
  jpa:
    hibernate:
      ddl-auto: update

